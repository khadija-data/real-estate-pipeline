name: 'real_estate_dw'
version: '1.0.0'
config-version: 2

profile: 'real_estate_dw'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"

clean-targets:
    - "target"
    - "dbt_packages"

models:
  real_estate_dw:
    bronze:
      +materialized: view
      +schema: BRONZE

    silver:
      +materialized: table
      +schema: SILVER

    gold:
      +materialized: table
      +schema: GOLD